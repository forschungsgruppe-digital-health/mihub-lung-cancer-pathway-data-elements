# Python-Abhängigkeiten für die Generator- und Validierungs-Skripte unter scripts/.
#
# Installation in eine lokale virtuelle Umgebung:
#   python3 -m venv .venv
#   source .venv/bin/activate          # Linux/macOS
#   .venv\Scripts\activate             # Windows PowerShell
#   pip install -r requirements.txt
#
# Versionen sind als Mindest-Versionen gepinnt (kompatibel mit Python ≥ 3.10).

pyyaml>=6.0
jsonschema>=4.20

# Nur für die Excel-Erhebungs-Vorlage (Klinik-Spur):
#   scripts/build-elicitation-workbook.py / scripts/import-elicitation-workbook.py
# Für validate.py + build-catalog.py NICHT nötig (die CI installiert nur pyyaml+jsonschema).
openpyxl>=3.1
